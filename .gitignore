# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove if you want to check in your vendor directory)
vendor/

# Go workspace file
go.work
go.work.sum

# IDE specific files
.idea/
.vscode/
*.swp
*.swo

# Environment variables (CRITICAL: keeps your DB passwords off GitHub)
.env
*.env

# OS generated files
.DS_Store
.DS_Store?
ehthumbs.db
Thumbs.db

# Project-specific binaries
# If you build your app using 'go build -o main', this ignores that file
/main
/api-server